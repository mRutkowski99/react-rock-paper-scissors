import styled from "styled-components";

const StyledGameChoice = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 3rem;

  h3 {
    text-transform: uppercase;
    letter-spacing: 1px;
    color: #fff;
  }
`;

export default StyledGameChoice;
